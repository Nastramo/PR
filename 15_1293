#include <iostream>


using namespace std;

int main() {
    setlocale(LC_ALL, "Russian");
    int A;
    int B;
    int N;
    int C;
    cin >> N;
    cin >> A;
    cin >> B;
    C = (A * B * N) * 2;
    cout << C;
}  

