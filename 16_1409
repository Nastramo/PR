#include <iostream>


using namespace std;

int main() {
    setlocale(LC_ALL, "Russian");
    int a;
    int b;
    int c;
    cin >> a;
    cin >> b;
    if ((a-1) + (b-1)+1 <= 10) {
        c = (a - 1) + (b - 1) + 1;
        a = c - a;
        b = c - b;
        cout << a << endl;
        cout << b << endl;
    }
}
